#! container

Container { 
  v64[] arr;
  v64[] varr;
  list<v64> l;
  set<v64> s;
  map<string, v64, v64> f;

  set<SomethingElse> someSet;

  /** add an auto field to check correct merging */
  auto map<SomethingElse, SomethingElse> z;
}

/** no instance of this is required */
SomethingElse{
}

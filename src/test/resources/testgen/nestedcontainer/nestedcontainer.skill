#! nestedcontainer

NestedContainer { 
  v64[][] a;
  list<list<v64>> l;
  map<v64, map<v64, v64>> m;
}

NestedContainerString {
  string[][] a;
  list<list<string>> l;
  map<string, map<string, string>> m;
}

A {}

NestedContainerA {
  A[][] a;
  list<list<A>> l;
  map<A, map<A, A>> m;
}
